// TODO: Implement Add Student Form / Modal
// Fields: Full Name, Email, Phone, Password, Assign Courses
// Saves to shared students data (later: API call)
export default function AddStudentForm({ onClose }) {
  return <div>Add Student Form — Coming Soon</div>
}
